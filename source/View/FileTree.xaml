<UserControl x:Class="Sidecab.View.FileTree"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lc="clr-namespace:Sidecab.View"
    xmlns:pr="clr-namespace:Sidecab.Presenter"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <ComboBox x:Name="comboBox_DriveSelector" Grid.Row="0" HorizontalAlignment="Stretch" ItemsSource="{Binding DriveSelector.DriveList}" SelectedItem="{Binding DriveSelector.CurrentDrive}" DisplayMemberPath="Caption" Focusable="False">
        </ComboBox>

        <TreeView x:Name="treeView_Directories" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" ItemsSource="{Binding RootDirectory.Children}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="pr:Directory" ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}" VerticalAlignment="Stretch" Margin="2,4" MouseDown="TextBlock_MouseDown"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>

        </TreeView>

    </Grid>

</UserControl>
